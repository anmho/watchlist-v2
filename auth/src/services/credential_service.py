from src.models import UserCredential, Session

from typing import Optional


# def create_credentials(session: Session, email: str, password: Optional[str] = None):

#     user_credential = UserCredential(email=email)

#     if password:
#         user_credential.set_password(password)

#     session.add(user_credential)
